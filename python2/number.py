count = 100
if count < 0:
	message = "is less than 0"
	print(count,message)
elif count < 100:
	message = "is less than 100"
	print(count,message)
else:
	message = "greater than or equal to 100"
	print(count,message)
